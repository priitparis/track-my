# Automatic tracking methods

This directory holds tracking methods that record location automatically,
without a manual button press. Each method gets its own subdirectory,
following the same pattern as [../manual/](../manual/).

## Methods

- [aisstream/](aisstream/) — polls AISStream.io's AIS WebSocket API for a
  ship's position on a GitHub Actions cron schedule.
